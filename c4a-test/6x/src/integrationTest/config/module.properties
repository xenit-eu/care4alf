module.id=${project.name}
module.title=${project.name}
module.description=${project.description}
module.version=${project.version.replace("-SNAPSHOT", "").replace("-", ".")}

module.repo.version.min=6.0
module.repo.version.max=6.9999
